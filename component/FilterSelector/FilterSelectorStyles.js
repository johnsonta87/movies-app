import styled from 'styled-components';

export const FilterSelectorStyle = styled.div`
  display: inline-flex;
  grid-gap: 0.5em;
  border-radius: 999px;
  border: 1px solid #000;
  overflow: hidden;
`;

export const FilterButtonStyle = styled.button`
  cursor: pointer;
  background: none;
  text-align: center;
  font-weight: 600;
  padding: 1em;
  border: 0;
  text-transform: uppercase;
`;
